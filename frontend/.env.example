# Copy to .env and fill in values — do NOT commit .env to version control.

VITE_API_URL=http://localhost:8000

# Must match API_KEY in backend/.env
# Leave empty if the backend has no API_KEY set (auth disabled).
VITE_API_KEY=
